import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Cart = () => {
  return (
    <SafeAreaView>
      <Text>cart</Text>
    </SafeAreaView>
  );
};

export default Cart;
